# steps:
#   # 1. Get the short commit SHA to use as a tag
#   - name: "gcr.io/cloud-builders/gcloud"
#     id: "get-commit-sha"
#     entrypoint: "bash"
#     args:
#       - "-c"
#       - |
#         echo "export BUILD_TAG=${_SHORT_SHA}" >> /workspace/build.env

#   # 2. Build the Docker image and tag it with the commit SHA
#   - name: "gcr.io/cloud-builders/docker"
#     id: "build-and-tag-image"
#     args:
#       - "build"
#       - "-t"
#       - "gcr.io/${_PROJECT_ID}/cloud-run-ex:${_SHORT_SHA}"
#       - "."

#   # 3. Push the tagged image to Container Registry
#   - name: "gcr.io/cloud-builders/docker"
#     id: "push-image"
#     args:
#       - "push"
#       - "gcr.io/${_PROJECT_ID}/cloud-run-ex:${_SHORT_SHA}"

# # Save the build tag as an output, so Terraform can use it
# # This is where the magic happens for Terraform to pick up the tag
# # In a more advanced setup, you'd use a service like Cloud Functions or
# # a dedicated script to update Terraform variables.
# # For simplicity, we'll assume Terraform will be run manually
# # with the tag as an input variable.
# options:
#   machineType: "e2-medium"